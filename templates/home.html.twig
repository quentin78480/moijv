{% extends "base.html.twig" %}

{% block body %}
    {# Commentaire #}
    {# {{ msg }} = <?= $msg ?> #}
          <header class="jumbotron my-4">
        <h1 class="display-3">Ma boutique-MoiJV</h1>
        <p class="lead">La boutique du Future.</p>
        <a href="{{ path('add_product')}}" class="btn btn-primary btn-lg">Ajouter un produit</a>
      </header>
      <div class="row text-center">
    {% include 'incs/product_list.html.twig' with { 'products' : products, 'rootName' : 'home_paginated' } %}
        </div>

        <div class="d-flex justify-content-center">
            {{ pagerfanta(products, 'twitter_bootstrap4', {'routeName' : 'home_paginated'})}}  
        </div>

{% endblock %}